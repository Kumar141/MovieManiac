const initialState = {
  posts: [],
  post: {},
  loading: false,
};

export default function func(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
